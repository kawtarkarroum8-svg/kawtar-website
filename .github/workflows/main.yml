name: Deploy Website to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # تسجيل الدخول التلقائي لـ AWS باستخدام مفاتيحك السحرية
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    # إرسال الملفات تلقائياً إلى مساحة كوثر كلود بروجكت 2026
    - name: Deploy to S3
      run: |
        aws s3 sync . s3://kawtar-cloud-project-2026 --delete


2 Tem 2026 Per 01:25 tarihinde Kawtar Karroum <kawtarkarroum8@gmail.com> şunu yazdı:
